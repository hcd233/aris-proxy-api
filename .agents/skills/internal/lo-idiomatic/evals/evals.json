{
  "skill_name": "lo-idiomatic",
  "evals": [
    {
      "id": 1,
      "prompt": "帮我把这段 Go 代码用 lo 简化：\n\n```go\nfunc getActiveUserEmails(users []User) []string {\n    var emails []string\n    for _, u := range users {\n        if u.Active && u.Email != \"\" {\n            emails = append(emails, u.Email)\n        }\n    }\n    return emails\n}\n```",
      "expected_output": "应使用 lo.FilterMap 一行替代 for+if+append 模式",
      "files": []
    },
    {
      "id": 2,
      "prompt": "我在 endpoint_repository.go 里看到这段代码，能否用 lo 简化？\n\n```go\nuserMap := make(map[uint]*dbmodel.User)\nfor _, u := range users {\n    userMap[u.ID] = u\n}\n```\n\n还有：\n```go\nids := make([]uint, 0, len(m))\nfor id := range m {\n    ids = append(ids, id)\n}\n```",
      "expected_output": "第一段应使用 lo.SliceToMap，第二段应使用 lo.Keys",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Go 代码里有很多 if ptr != nil { val = *ptr } 的模式，怎么用 lo 简化？还有初始化 DB 时 if err != nil { panic(err) } 能否更简洁？",
      "expected_output": "应推荐 lo.FromPtr/lo.ToPtr 处理指针，lo.Must/lo.Must0/lo.Must1 处理启动阶段的 panic-on-error",
      "files": []
    }
  ]
}
