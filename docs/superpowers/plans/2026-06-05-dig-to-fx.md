# dig → fx 迁移 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将项目依赖注入从 `go.uber.org/dig` 迁移到 `go.uber.org/fx`，消除手动生命周期管理代码和包级全局变量

**Architecture:** fx.New() 替代 dig.New()，新文件 `internal/bootstrap/lifecycle.go` 集中注册生命周期 hook，新目录 `internal/bootstrap/modules/` 按层分组 provider，`cmd/server.go` 精简到 ~30 行

**Tech Stack:** go.uber.org/fx v1, Go 1.25.1

---

### Task 0: 创建 worktree 和分支

**Files:**
- 创建 worktree: `.worktrees/dig-to-fx-2026-06-05`
- 创建分支: `refactor/dig-to-fx-2026-06-05`

- [ ] **Step 1: 创建 worktree 并切换分支**

```bash
git worktree add .worktrees/dig-to-fx-2026-06-05 -b refactor/dig-to-fx-2026-06-05
```

- [ ] **Step 2: 确认状态**

```bash
git branch --show-current
```
Expected: `refactor/dig-to-fx-2026-06-05`

---

### Task 1: 添加 fx 依赖

**Files:**
- Modify: `go.mod`

- [ ] **Step 1: 安装 fx**

```bash
go get go.uber.org/fx@v1
go mod tidy
```

- [ ] **Step 2: 验证编译**

```bash
go build ./...
```
Expected: 编译通过（dig 仍存在，无冲突）

- [ ] **Step 3: 提交**

```bash
git add go.mod go.sum
git commit -m "chore: add go.uber.org/fx dependency"
```

---

### Task 2: 消除 jwt 包级 singleton

**Files:**
- Modify: `internal/infrastructure/jwt/singleton.go`

将 `init()` 中的包级变量改为可从外部调用的构造函数，保留 `GetAccessTokenSigner()` 和 `GetRefreshTokenSigner()` 直到后续 task 移除。

- [ ] **Step 1: 重写 singleton.go**

```go
package jwt

import (
	"github.com/hcd233/aris-proxy-api/internal/config"
)

func NewAccessTokenSigner() TokenSigner {
	return newTokenSigner(config.JwtAccessTokenSecret, config.JwtAccessTokenExpired)
}

func NewRefreshTokenSigner() TokenSigner {
	return newTokenSigner(config.JwtRefreshTokenSecret, config.JwtRefreshTokenExpired)
}

func newTokenSigner(secret string, expired time.Duration) TokenSigner {
	return &tokenSigner{
		JwtTokenSecret:  secret,
		JwtTokenExpired: expired,
	}
}
```

删除原有 `var (accessTokenSvc, refreshTokenSvc)` 和 `func init()` 和 `func GetAccessTokenSigner()` 和 `func GetRefreshTokenSigner()`。

需要新增 import: `"time"`。

- [ ] **Step 2: 更新 container.go 中的 newAccessTokenSigner/newRefreshTokenSigner**

当前代码（line 523-528）：
```go
func newAccessTokenSigner() identityservice.TokenSigner {
    return jwt.GetAccessTokenSigner()
}

func newRefreshTokenSigner() identityservice.TokenSigner {
    return jwt.GetRefreshTokenSigner()
}
```

改为：
```go
func newAccessTokenSigner() identityservice.TokenSigner {
    return jwt.NewAccessTokenSigner()
}

func newRefreshTokenSigner() identityservice.TokenSigner {
    return jwt.NewRefreshTokenSigner()
}
```

- [ ] **Step 3: 更新 JwtMiddleware 接受 accessSigner 参数**

修改 `internal/middleware/jwt.go` 的 `JwtMiddleware` 函数签名：

当前（line 51）：
```go
func JwtMiddleware(db *gorm.DB, cache *redis.Client) func(ctx huma.Context, next func(huma.Context)) {
    accessTokenSvc := jwt.GetAccessTokenSigner()
```

改为：
```go
func JwtMiddleware(db *gorm.DB, cache *redis.Client, accessTokenSvc jwt.TokenSigner) func(ctx huma.Context, next func(huma.Context)) {
```

删除 import 中的 `jwt.GetAccessTokenSigner()` 调用即可（import 本身还用于 `jwt.TokenSigner` 类型，保留）。

- [ ] **Step 4: 更新所有调用 JwtMiddleware 的 router 文件**

需要更新 `internal/router/user.go:15`、`apikey.go:17`、`endpoint.go:16`、`model.go:16`、`session.go:16`、`audit.go:17`。需要给这些 `initXxxRouter` 函数增加 `accessSigner jwt.TokenSigner` 参数。

`APIRouterDependencies` struct 增加字段：
```go
JwtAccessSigner identityservice.TokenSigner
```

但 router 包当前不 import `identityservice`（即 `internal/domain/identity/service`）。改为引入 `jwt.TokenSigner` 类型，因为 `jwt.NewAccessTokenSigner()` 返回的就是 `jwt.TokenSigner`。

实际上更简单做法：APIRouterDependencies 增加 `AccessSigner jwt.TokenSigner` 字段，因为 router 包已经使用了 middleware，而 middleware 需要 `jwt.TokenSigner` 类型。

修改 `internal/router/router.go`：

```go
import (
    ...
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/jwt"
    ...
)

type APIRouterDependencies struct {
    ...
    AccessSigner     jwt.TokenSigner
}
```

然后每个 `initXxxRouter` 函数接受 deps 并将 `deps.AccessSigner` 传给 `JwtMiddleware`。

修改 `internal/router/user.go`：
```go
func initUserRouter(group *huma.Group, userHandler handler.UserHandler, db *gorm.DB, cache *redis.Client, accessSigner jwt.TokenSigner) {
    group.UseMiddleware(middleware.JwtMiddleware(db, cache, accessSigner))
    ...
}
```

其余 5 个 router 文件同 pattern。

`internal/router/router.go` 中各调用处改为传入 `deps.AccessSigner`：
```go
initUserRouter(userGroup, deps.UserHandler, deps.DB, deps.Cache, deps.AccessSigner)
```

- [ ] **Step 5: 更新 routeParams 和 RegisterRoutes 传递 AccessSigner**

修改 `internal/bootstrap/router.go` 的 `routeParams` struct 增加字段：
```go
type routeParams struct {
    dig.In
    ...
    AccessSigner     identityservice.TokenSigner `name:"accessSigner"`
}
```

修改 `router.RegisterAPIRouter` 调用处增加赋参：
```go
router.RegisterAPIRouter(server.HumaAPI, router.APIRouterDependencies{
    ...
    AccessSigner:    params.AccessSigner,
})
```

- [ ] **Step 6: 验证编译和测试**

```bash
go build ./...
go test -count=1 ./...
```

- [ ] **Step 7: 提交**

```bash
git add internal/infrastructure/jwt/singleton.go internal/bootstrap/container.go internal/middleware/jwt.go internal/router/ internal/bootstrap/router.go
git commit -m "refactor: eliminate jwt singleton, inject signer via parameters"
```

---

### Task 3: 消除 inflight singleton

**Files:**
- Modify: `internal/common/inflight/tracker.go`
- Modify: `internal/middleware/inflight.go`
- Modify: `internal/handler/ping.go`
- Modify: `internal/bootstrap/container.go`
- Modify: `cmd/server.go`

- [ ] **Step 1: 修改 tracker.go — 新增 NewTracker(), 删除 InitTracker()/GetTracker()**

```go
// 新增
func NewTracker() *Tracker {
    return &Tracker{state: atomic.Int32{}, wg: sync.WaitGroup{}}
}

// 删除以下内容:
// var tracker *Tracker
// func InitTracker() { tracker = newTracker() }
// func GetTracker() *Tracker { return tracker }
```

注意 `state` 字段的初始化（原 `newTracker()` 函数中）：
```go
func newTracker() *Tracker {
    t := &Tracker{}
    t.state.Store(constant.InflightStateRunning)
    return t
}
```

应保留 `newTracker` 逻辑，放入 `NewTracker()` 中。

- [ ] **Step 2: 修改 InflightMiddleware 接受 *Tracker 参数**

```go
func InflightMiddleware(tracker *inflight.Tracker) fiber.Handler {
    return func(c fiber.Ctx) error {
        ...
        if !tracker.Track() {
```

删除内部 `inflight.GetTracker()` 调用。

- [ ] **Step 3: 修改 pingHandler 接受 *Tracker 参数**

`internal/handler/ping.go` 的 `pingHandler` struct 增加字段：
```go
type pingHandler struct {
    tracker *inflight.Tracker
}
```

`NewPingHandler` 接受参数：
```go
func NewPingHandler(tracker *inflight.Tracker) PingHandler {
    return &pingHandler{tracker: tracker}
}
```

`HandleReady` 中使用 `h.tracker` 代替 `inflight.GetTracker()`。

- [ ] **Step 4: 更新 container.go 中 inflight 相关的 provider**

删除 `container.go` 中 `newTracker` provider（如果有的话），改为：
```go
func provideTracker(container *dig.Container) error {
    return container.Provide(inflight.NewTracker)
}
```

- [ ] **Step 5: 更新 cmd/server.go 中 InflightMiddleware 调用**

```go
app.Use(
    middleware.InflightMiddleware(inflightTracker),
    ...
)
```

需要从 infra 或 container 获取 tracker。当前在 `cmd/server.go` 中也可以通过 `inflight.GetTracker()` 但后续该函数已删除。改为在 `InitInfrastructure()` / `BuildServer()` 中返回 tracker，或在 `RegisterRoutes` 之后通过 container Invoke 获取。

更简洁的方式：在 `container.go` 中增加一个 provider 返回 `*inflight.Tracker`，然后在 `RegisterRoutes` 之后通过另一个 `Invoke` 提取 tracker 用于中间件注册。

对于 `cmd/server.go` 中的 `InflightMiddleware()` 调用 — 当前它在 `RegisterRoutes` 之前作为 middleware 注册。Tracker 需要通过构造函数注入。最简单的办法是直接让 `InflightMiddleware` 接受 `*inflight.Tracker`，然后在 `cmd/server.go` 中从 `Infrastructure` 或通过 `BuildServer` 返回。

让我们修改 `bootstrap.Server` struct 来包含 tracker：
```go
type Server struct {
    container *dig.Container
    App       *fiber.App
    HumaAPI   huma.API
    Tracker   *inflight.Tracker
}
```

在 `BuildServer` 的 `Invoke` 中提取 tracker：
```go
var server *Server
if err := container.Invoke(func(app *fiber.App, humaAPI huma.API, tracker *inflight.Tracker) {
    server = &Server{container: container, App: app, HumaAPI: humaAPI, Tracker: tracker}
}); err != nil {
    return nil, err
}
```

然后在 `cmd/server.go` 中：
```go
app.Use(
    middleware.InflightMiddleware(server.Tracker),
    ...
)
```

- [ ] **Step 6: 验证编译和测试**

```bash
go build ./...
go test -count=1 ./...
```

- [ ] **Step 7: 提交**

```bash
git add internal/common/inflight/tracker.go internal/middleware/inflight.go internal/handler/ping.go internal/bootstrap/container.go cmd/server.go
git commit -m "refactor: eliminate inflight singleton, inject tracker"
```

---

### Task 4: 消除 pool singleton

**Files:**
- Modify: `internal/infrastructure/pool/pool.go`
- Modify: `internal/bootstrap/container.go`
- Modify: `cmd/server.go`

- [ ] **Step 1: 删除 poolManager 包级变量和 GetPoolManager()/InitPoolManager()/StopPoolManager()**

从 `internal/infrastructure/pool/pool.go` 删除：
```go
var poolManager *PoolManager
func GetPoolManager() *PoolManager { ... }
func InitPoolManager(db *gorm.DB) *PoolManager { ... }
func StopPoolManager() { ... }
```

保留 `NewPoolManager(db *gorm.DB) *PoolManager`。

新增 `StopWithContext` 方法：
```go
func (pm *PoolManager) StopWithContext(ctx context.Context) error {
    done := make(chan struct{})
    go func() {
        defer close(done)
        pm.Stop()
    }()
    select {
    case <-done:
        return nil
    case <-ctx.Done():
        return ctx.Err()
    }
}
```

- [ ] **Step 2: 更新 container.go 中的 newTaskSubmitter**

当前（line 512-514）：
```go
func newTaskSubmitter() usecase.TaskSubmitter {
    return pool.GetPoolManager()
}
```

改为接受 PoolManager 注入：
```go
func newTaskSubmitter(pm *pool.PoolManager) usecase.TaskSubmitter {
    return pm
}
```

删除 container.go 中手动提供 PoolManager 的代码（已在 `provideInfrastructure` 中通过 infra 注入）。

- [ ] **Step 3: 更新 InitInfrastructure 和 cmd/server.go**

`InitInfrastructure()` 不再调用 `pool.InitPoolManager()`，改为调用 `pool.NewPoolManager(db)` 并将结果存入 `Infrastructure`。

`cmd/server.go` 中的 `gracefulShutdown` 不再调用 `pool.StopPoolManager()`。这一步稍后在 lifecycle task 中统一处理。

- [ ] **Step 4: 验证编译和测试**

```bash
go build ./...
go test -count=1 ./...
```

- [ ] **Step 5: 提交**

```bash
git add internal/infrastructure/pool/pool.go internal/bootstrap/container.go cmd/server.go
git commit -m "refactor: eliminate pool singleton, use constructor injection"
```

---

### Task 5: 消除 cron singleton

**Files:**
- Modify: `internal/cron/cron.go`
- Modify: `internal/bootstrap/container.go`
- Modify: `cmd/server.go`

- [ ] **Step 1: 修改 InitCronJobs 返回 []Cron，不再存储包级变量**

将 `InitCronJobs` 改为返回创建的 cron 实例：
```go
func InitCronJobs(parentCtx context.Context, db *gorm.DB, poolManager *pool.PoolManager, cache *redis.Client, thinkRepo conversation.ThinkExtractRepository) []Cron {
    ...
    for _, entry := range entries {
        ...
        c := entry.Factory(db, poolManager, cache, thinkRepo)
        lo.Must0(c.Start())
        crons = append(crons, c)
        ...
    }
    return crons
}
```

删除 `var cronInstances []Cron`，删除 `StopCronJobs()`、`CronInstanceCount()`，新增：
```go
func StopCronJobsWithContext(ctx context.Context, crons []Cron) error {
    done := make(chan struct{})
    go func() {
        defer close(done)
        for _, c := range crons {
            c.Stop()
        }
    }()
    select {
    case <-done:
        return nil
    case <-ctx.Done():
        return ctx.Err()
    }
}
```

- [ ] **Step 2: 更新 InitInfrastructure 和 BuildServer**

`InitInfrastructure` 中改为返回 crons 列表（存入 Infrastructure 或通过 BuildServer 返回）。简单做法：在 `Infrastructure` struct 中增加字段：
```go
type Infrastructure struct {
    DB          *gorm.DB
    Cache       *redis.Client
    PoolManager *pool.PoolManager
    CronJobs    []cron.Cron
}
```

- [ ] **Step 3: 更新 cmd/server.go gracefulShutdown**

不再调用 `cron.StopCronJobs()`，改为使用 infra.CronJobs 参数。后续在 lifecycle 统一处理。

- [ ] **Step 4: 验证编译和测试**

```bash
go build ./...
go test -count=1 ./...
```

- [ ] **Step 5: 提交**

```bash
git add internal/cron/cron.go internal/bootstrap/container.go cmd/server.go
git commit -m "refactor: eliminate cron singleton, return cron list explicitly"
```

---

### Task 6: 重写 bootstrap 容器为 fx 风格

这是核心迁移 task。需要在保留当前代码可工作的前提下，逐步引入 fx。

**Files:**
- Create: `internal/bootstrap/modules/infra.go`
- Create: `internal/bootstrap/modules/repository.go`
- Create: `internal/bootstrap/modules/application.go`
- Create: `internal/bootstrap/modules/handler.go`
- Create: `internal/bootstrap/modules/cron.go`
- Create: `internal/bootstrap/lifecycle.go`
- Modify: `internal/bootstrap/container.go`
- Modify: `cmd/server.go`

**重要原则**: 在切换到 fx.New() 之前，先用 fx.Provide + fx.Invoke 替换 dig API，保持 `BuildServer` 返回 `*Server` 的签名不变。

- [ ] **Step 1: 创建 modules/infra.go**

```go
package modules

import (
    "github.com/hcd233/aris-proxy-api/internal/common/inflight"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/cache"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/database"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/httpclient"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/pool"
    "go.uber.org/fx"
    "gorm.io/gorm"
    "github.com/redis/go-redis/v9"
)

var InfraModule = fx.Module("infrastructure",
    fx.Provide(
        NewDB,
        NewCache,
        NewPoolManager,
        NewInflightTracker,
    ),
    fx.Invoke(InitHTTPClient),
)

func NewDB() *gorm.DB {
    return database.InitDatabase()
}

func NewCache() *redis.Client {
    return cache.InitCache()
}

func NewPoolManager(db *gorm.DB) *pool.PoolManager {
    return pool.NewPoolManager(db)
}

func NewInflightTracker() *inflight.Tracker {
    return inflight.NewTracker()
}

func InitHTTPClient() {
    httpclient.InitHTTPClient()
}
```

- [ ] **Step 2: 创建 modules/repository.go**

将 container.go 中所有 `newXxxRepository` 和基础设施 provider（transport, oauth2 platforms, state manager, cache 等）移到该文件。

```go
package modules

import (
    "go.uber.org/fx"

    "github.com/hcd233/aris-proxy-api/internal/config"
    "github.com/hcd233/aris-proxy-api/internal/domain/apikey"
    apikeyservice "github.com/hcd233/aris-proxy-api/internal/domain/apikey/service"
    "github.com/hcd233/aris-proxy-api/internal/domain/identity"
    identityservice "github.com/hcd233/aris-proxy-api/internal/domain/identity/service"
    "github.com/hcd233/aris-proxy-api/internal/domain/llmproxy"
    "github.com/hcd233/aris-proxy-api/internal/domain/modelcall"
    oauth2service "github.com/hcd233/aris-proxy-api/internal/domain/oauth2/service"
    "github.com/hcd233/aris-proxy-api/internal/domain/session"
    oauth2port "github.com/hcd233/aris-proxy-api/internal/application/oauth2/port"
    "github.com/hcd233/aris-proxy-api/internal/application/llmproxy/usecase"
    "github.com/hcd233/aris-proxy-api/internal/common/enum"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/cache"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/jwt"
    infraoauth2 "github.com/hcd233/aris-proxy-api/internal/infrastructure/oauth2"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/repository"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/transport"
    llmproxyservice "github.com/hcd233/aris-proxy-api/internal/domain/llmproxy/service"
    sessionport "github.com/hcd233/aris-proxy-api/internal/application/session/port"
    "gorm.io/gorm"
    "github.com/redis/go-redis/v9"
)

var RepositoryModule = fx.Module("repository",
    fx.Provide(
        NewUserRepository,
        NewAPIKeyRepository,
        NewSessionReadRepository,
        NewSessionWriteRepository,
        NewAuditRepository,
        NewEndpointRepository,
        NewModelRepository,
        NewEndpointReadRepository,
        NewAudioDirCreator,
        NewShareCache,
        NewSessionDetailCache,
        NewOpenAIProxy,
        NewAnthropicProxy,
        NewAPIKeyGenerator,
        NewOauth2Platforms,
        NewStateManager,
        NewAccessTokenSigner,
        NewRefreshTokenSigner,
        NewEndpointResolver,
    ),
)

// ... 各构造函数实现从 container.go 原样搬运 ...
```

注意 `NewAccessTokenSigner` 和 `NewRefreshTokenSigner` 需要使用 `fx.ResultTags`：
```go
func NewAccessTokenSigner() fx.Annotated {
    return fx.Annotate(
        func() identityservice.TokenSigner { return jwt.NewAccessTokenSigner() },
        fx.ResultTags(`name:"accessSigner"`),
    )
}

func NewRefreshTokenSigner() fx.Annotated {
    return fx.Annotate(
        func() identityservice.TokenSigner { return jwt.NewRefreshTokenSigner() },
        fx.ResultTags(`name:"refreshSigner"`),
    )
}
```

- [ ] **Step 3: 创建 modules/application.go**

搬运 container.go 中 `provideApplication` 及所有子函数的所有 provider。使用 `fx.Annotated` + `fx.ResultTags` 替代 `dig.Name`（已在 repository module 中处理 signer 命名）。

- [ ] **Step 4: 创建 modules/handler.go**

搬运 container.go 中 `provideHandlers` 及所有 handler 构造函数、dependencies 构造函数、adapter 类型。

- [ ] **Step 5: 创建 modules/cron.go**

```go
package modules

import (
    "context"

    "github.com/hcd233/aris-proxy-api/internal/cron"
    "github.com/hcd233/aris-proxy-api/internal/domain/conversation"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/pool"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/repository"
    "go.uber.org/fx"
    "gorm.io/gorm"
    "github.com/redis/go-redis/v9"
)

var CronModule = fx.Module("cron",
    fx.Provide(
        NewThinkExtractRepo,
        NewCronEntries,
    ),
)

func NewThinkExtractRepo(db *gorm.DB) conversation.ThinkExtractRepository {
    return repository.NewThinkExtractRepository(db)
}

func NewCronEntries(db *gorm.DB, poolManager *pool.PoolManager, cache *redis.Client, thinkRepo conversation.ThinkExtractRepository) []cron.Cron {
    return cron.InitCronJobs(context.Background(), db, poolManager, cache, thinkRepo)
}
```

注意 `cron.InitCronJobs` 内部会调用 `c.Start()`，这里创建 entry 时会立刻启动 cron（OnStart 在 fx lifecycle 之前）。后续在 lifecycle.go 中会用 fx lifecycle hook 管理 start/stop。

为了正确支持 lifecycle，`InitCronJobs` 需要改为不自动 start。但由于我们希望在 OnStart 时启动，可以保持当前行为（先启动），OnStop 时停止。或者改为：InitCronJobs 创建但不启动，在 lifecycle OnStart 中启动。

简化方案：保持 `InitCronJobs` 当前行为（创建并启动），lifecycle OnStop 中调用 `StopCronJobsWithContext`。

- [ ] **Step 6: 创建 lifecycle.go**

```go
package bootstrap

import (
    "context"
    "fmt"
    "time"

    "github.com/gofiber/fiber/v3"
    "github.com/hcd233/aris-proxy-api/internal/common/constant"
    "github.com/hcd233/aris-proxy-api/internal/common/inflight"
    "github.com/hcd233/aris-proxy-api/internal/cron"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/cache"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/database"
    "github.com/hcd233/aris-proxy-api/internal/infrastructure/pool"
    "github.com/hcd233/aris-proxy-api/internal/logger"
    "github.com/redis/go-redis/v9"
    "go.uber.org/fx"
    "go.uber.org/zap"
    "gorm.io/gorm"
)

type lifecycleParams struct {
    fx.In

    Lifecycle       fx.Lifecycle
    App             *fiber.App
    DB              *gorm.DB
    Cache           *redis.Client
    PoolManager     *pool.PoolManager
    InflightTracker *inflight.Tracker
    CronEntries     []cron.Cron
}

func registerLifecycleHooks(params lifecycleParams) {
    // OnStop hooks run in REVERSE order of registration.
    // Desired stop order: Cron → Pool → Inflight → HTTP → Logger → DB → Redis
    // So register in reverse: Redis → DB → Logger → HTTP → Inflight → Pool → Cron

    params.Lifecycle.Append(fx.Hook{
        OnStart: func(ctx context.Context) error { return nil },
        OnStop: func(ctx context.Context) error {
            return cache.CloseCache(params.Cache)
        },
    })

    params.Lifecycle.Append(fx.Hook{
        OnStart: func(ctx context.Context) error { return nil },
        OnStop: func(ctx context.Context) error {
            return database.CloseDatabase(params.DB)
        },
    })

    params.Lifecycle.Append(fx.Hook{
        OnStart: func(ctx context.Context) error { return nil },
        OnStop: func(ctx context.Context) error {
            return logger.Logger().Sync()
        },
    })

    params.Lifecycle.Append(fx.Hook{
        OnStart: func(ctx context.Context) error {
            go params.App.Listen(listenAddr)
            return nil
        },
        OnStop: func(ctx context.Context) error {
            shutdownCtx, cancel := context.WithTimeout(context.Background(), constant.FiberShutdownTimeout)
            defer cancel()
            return params.App.ShutdownWithContext(shutdownCtx)
        },
    })

    params.Lifecycle.Append(fx.Hook{
        OnStart: func(ctx context.Context) error { return nil },
        OnStop: func(ctx context.Context) error {
            params.InflightTracker.Drain(constant.InflightDrainTimeout)
            return nil
        },
    })

    params.Lifecycle.Append(fx.Hook{
        OnStart: func(ctx context.Context) error { return nil },
        OnStop: func(ctx context.Context) error {
            ctx, cancel := context.WithTimeout(context.Background(), constant.PoolStopTimeout)
            defer cancel()
            return params.PoolManager.StopWithContext(ctx)
        },
    })

    params.Lifecycle.Append(fx.Hook{
        OnStart: func(ctx context.Context) error {
            // cron entries are already created and started by InitCronJobs
            return nil
        },
        OnStop: func(ctx context.Context) error {
            ctx, cancel := context.WithTimeout(context.Background(), constant.CronStopTimeout)
            defer cancel()
            return cron.StopCronJobsWithContext(ctx, params.CronEntries)
        },
    })
}

var listenAddr string

func setListenAddr(host, port string) {
    listenAddr = fmt.Sprintf("%s:%s", host, port)
}
```

- [ ] **Step 7: 重写 container.go — BuildFxApp()**

将 `container.go` 精简为：

```go
package bootstrap

import (
    "go.uber.org/fx"

    "github.com/hcd233/aris-proxy-api/internal/bootstrap/modules"
    "github.com/hcd233/aris-proxy-api/internal/common/constant"
    "github.com/hcd233/aris-proxy-api/internal/config"

    "github.com/gofiber/fiber/v3"
    "github.com/danielgtaylor/huma/v2"
    "github.com/hcd233/aris-proxy-api/internal/api"
)

type Server struct {
    App     *fiber.App
    HumaAPI huma.API
}

func BuildFxApp(host, port string, customizers ...fx.Option) *fx.App {
    setListenAddr(host, port)
    opts := []fx.Option{
        modules.InfraModule,
        modules.RepositoryModule,
        modules.ApplicationModule,
        modules.HandlerModule,
        modules.CronModule,
        fx.Provide(
            api.NewFiberApp,
            api.NewHumaAPI,
        ),
        fx.Invoke(
            registerLifecycleHooks,
            registerRoutes,
        ),
        fx.StopTimeout(constant.ShutdownTimeout),
    }
    opts = append(opts, customizers...)
    return fx.New(opts...)
}

func registerRoutes(params routeParams) {
    if config.Env != "production" {
        router.RegisterDocsRouter(params.App)
    }
    router.RegisterAPIRouter(params.HumaAPI, router.APIRouterDependencies{
        DB:               params.DB,
        Cache:            params.Cache,
        PingHandler:      params.PingHandler,
        TokenHandler:     params.TokenHandler,
        Oauth2Handler:    params.Oauth2Handler,
        UserHandler:      params.UserHandler,
        APIKeyHandler:    params.APIKeyHandler,
        SessionHandler:   params.SessionHandler,
        EndpointHandler:  params.EndpointHandler,
        ModelHandler:     params.ModelHandler,
        AuditHandler:     params.AuditHandler,
        OpenAIHandler:    params.OpenAIHandler,
        AnthropicHandler: params.AnthropicHandler,
        AccessSigner:     params.AccessSigner,
    })
    router.RegisterWebRouter(params.App, web.DistFS)
}
```

同时删除以下不再需要的内容：
- `Infrastructure` struct 及其 `InitInfrastructure()` 函数
- `BuildServer()` 函数
- `provide()` 函数及所有 `provide*` 子函数
- 所有 `newXxx*` 构造函数（已搬迁到 modules/）
- 所有 adapter 类型及方法（已搬迁到 modules/）
- `openAIUseCaseAdapter`、`anthropicUseCaseAdapter`、`auditServiceAdapter`

- [ ] **Step 8: 更新 routeParams 移除 dig.In**

```go
type routeParams struct {
    fx.In
    DB               *gorm.DB
    Cache            *redis.Client
    App              *fiber.App
    HumaAPI          huma.API
    PingHandler      handler.PingHandler
    TokenHandler     handler.TokenHandler
    Oauth2Handler    handler.Oauth2Handler
    UserHandler      handler.UserHandler
    APIKeyHandler    handler.APIKeyHandler
    SessionHandler   handler.SessionHandler
    EndpointHandler  handler.EndpointHandler
    ModelHandler     handler.ModelHandler
    AuditHandler     handler.AuditHandler
    OpenAIHandler    handler.OpenAIHandler
    AnthropicHandler handler.AnthropicHandler
    AccessSigner     identityservice.TokenSigner `name:"accessSigner"`
}
```

将 `dig.In` 改为 `fx.In`。

- [ ] **Step 9: 验证编译**

```bash
go build ./...
```

根据编译错误修正遗漏的 import 或未搬迁的 provider。

- [ ] **Step 10: 提交**

```bash
git add -A
git commit -m "refactor: rewrite bootstrap to use fx, split into modules"
```

---

### Task 7: 重写 cmd/server.go

**Files:**
- Modify: `cmd/server.go`

将 `gracefulShutdown` 函数完全删除，`startServerCmd` 的 `Run` 精简为：

```go
var startServerCmd = &cobra.Command{
    Use:   "start",
    Short: "Start the API server",
    Long:  `Start and run the API server, listening on the specified host and port`,
    Run: func(cmd *cobra.Command, _ []string) {
        defer func() {
            if r := recover(); r != nil {
                logger.Logger().Error("[Server] Start server panic", zap.Any("error", r), zap.ByteString("stack", debug.Stack()))
                os.Exit(1)
            }
        }()
        host, port := lo.Must1(cmd.Flags().GetString("host")), lo.Must1(cmd.Flags().GetString("port"))

        logger.Logger().Info("[Server] Environment",
            zap.String("env", config.Env),
            zap.Duration("readTimeout", config.ReadTimeout),
            zap.Duration("writeTimeout", config.WriteTimeout),
            zap.Int("maxHeaderBytes", config.MaxHeaderBytes),
            zap.Int("poolStoreWorkers", config.Pool.Store.Workers),
            zap.Int("poolStoreQueueSize", config.Pool.Store.QueueSize),
            zap.Int("poolAgentWorkers", config.Pool.Agent.Workers),
            zap.Int("poolAgentQueueSize", config.Pool.Agent.QueueSize),
            zap.Int("sqlBatchSize", config.SQLBatchSize),
        )

        app := bootstrap.BuildFxApp(host, port)
        app.Run()
    },
}
```

同时更新 import — 删除 `"os/signal"`、`"syscall"`、`fiber`、`samber/lo`（如果不再使用）、`inflight`、`cron`、`pool`、`database`、`cache`、`constant` 等不再需要的。

- [ ] **Step 2: 验证编译**

```bash
go build ./...
```

- [ ] **Step 3: 提交**

```bash
git add cmd/server.go
git commit -m "refactor: simplify cmd/server.go to use fx app lifecycle"
```

---

### Task 8: 清理 dig 残留

**Files:**
- Modify: `go.mod`

- [ ] **Step 1: 移除 dig 依赖**

```bash
go mod tidy
```

如果 `go mod tidy` 自动移除了 dig，则完成。否则手动执行：
```bash
go get go.uber.org/dig@none
go mod tidy
```

- [ ] **Step 2: 搜索代码中是否还有 dig 引用**

```bash
rg "go.uber.org/dig" --type go
```
Expected: 无结果（除测试文件可能仍引用 old test utils）

- [ ] **Step 3: 验证编译**

```bash
go build ./...
```

- [ ] **Step 4: 提交**

```bash
git add go.mod go.sum
git commit -m "chore: remove go.uber.org/dig dependency"
```

---

### Task 9: 修复测试

**Files:**
- Modify: `test/e2e/` 下的测试文件
- Modify: `test/unit/` 下的测试文件

- [ ] **Step 1: 运行全量测试查看失败**

```bash
go test -count=1 ./... 2>&1 | tee test-output.log
```

- [ ] **Step 2: 逐个修复测试**

对每个需要 bootstrap 的测试，使用 `fxtest.New` 替代手动构建：

```go
import "go.uber.org/fx/fxtest"

func TestXxx(t *testing.T) {
    var server *bootstrap.Server
    app := fxtest.New(t,
        bootstrap.BuildFxApp("localhost", "0").Options()...,
        fx.Populate(&server),
    )
    defer app.RequireStart().RequireStop()
    // ... use server
}
```

对单元测试中受 sigleton 消除影响的，访问方式由 `pool.GetPoolManager()` 改为构造函数注入。

- [ ] **Step 3: 确保全量测试通过**

```bash
go test -count=1 ./...
```
Expected: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add test/
git commit -m "test: update tests for fx bootstrap migration"
```

---

### Task 10: 最终验证

- [ ] **Step 1: 完整构建**

```bash
make build
```
Expected: 成功生成二进制

- [ ] **Step 2: lint 检查**

```bash
make lint
```
Expected: 零告警

- [ ] **Step 3: 全量测试**

```bash
go test -count=1 ./...
```
Expected: 全部 PASS

- [ ] **Step 4: 启动验证**

```bash
go run main.go server start --host localhost --port 8080
```
然后 Ctrl+C，确认日志输出 8 步关闭顺序正确。

- [ ] **Step 5: E2E 测试**

```bash
go test -v -count=1 ./test/e2e/...
```
Expected: 全部 PASS

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "chore: final verification - build, lint, test all pass"
```

---

## 自审

**Spec 覆盖检查：** 每个设计文档中的目标都有对应 task：
- API 替换：Task 6（fx.Module + fx.Provide/Invoke）
- 生命周期下沉：Task 6 Step 6（lifecycle.go）
- 包级 singleton 消除：Task 2（jwt）、Task 3（inflight）、Task 4（pool）、Task 5（cron）
- `dig.Name` → `fx.ResultTags`：Task 6 Step 2
- K8s preStop 配合：lifecycle.go 中 Inflight OnStop 保留 draining 逻辑

**占位符检查：** 所有步骤都有实际代码或命令，无 TBD/TODO。

**类型一致性：** `TokenSigner` 从 `jwt` 和 `identityservice` 两个包，接口签名一致，跨引用无冲突。`fx.In` → `fx.Out` 语法与 dig 完全兼容。
