# 2026-06-23

## 回滚 master 深色模式改动

- 深色模式提交 `719012e feat(web): add light/dark/system theme toggle` 为 master 最新提交，已推送到 origin/master
- 改动涉及 4 个文件：`web/src/app/(dashboard)/layout.tsx`、`web/src/app/layout.tsx`、`web/src/components/theme-provider.tsx`(新增)、`web/src/components/theme-toggle.tsx`(新增)
- 用户选择 `git revert` 方式（保留历史，不改写远程）
- 执行 `git revert --no-edit 719012e`，生成新提交 `4cd817b Revert "feat(web): add light/dark/system theme toggle"`
- 用户确认后执行 `git push origin master`，推送成功（719012e..4cd817b）
- 推送后触发 `docker-publish.yml` 自动构建镜像并部署到 K8s，无需手动部署
